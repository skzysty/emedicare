import 'package:flutter/material.dart';

class DeploymentPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Scaffold(
        appBar: AppBar(
          title: Text('Legal and Privacy Policy'),
          centerTitle: true,
          backgroundColor: Colors.teal,
        ),
      );
}
